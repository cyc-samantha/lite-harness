<!--
  plan.md.tmpl — planner output template (PLAN.md §1 repo layout, §6 step 3).

  WHERE THIS FILE LIVES ON DISK (in git, unlike STATE.md):
    <worktree>/plan.md, committed to the `lite/<slug>` branch.

  This is the durable, git-committed half of the memory/resume split (see
  `templates/README.md`). It must survive compaction and session death on
  its own — a software-engineer or qa-engineer resuming from a cold start
  with only `git log` + this file must be able to continue the run.

  This shape mirrors `agents/planner.md`'s own "Output Format" section
  verbatim (do not let the two drift) and is written so both downstream
  agents can consume it directly:
    - software-engineer reads each slice's AC checklist + Files Touched
      note to know exactly what to build and where.
    - qa-engineer reads the same AC checklist as its coverage-gap map
      ("cross-check every AC ... against the software-engineer's diff") —
      one unchecked AC in a slice is one coverage gap to close or file.

  FILL-IN CONVENTION: replace every `<angle-bracket>` placeholder.
-->
# Plan: <one-line idea>

## Slices
- [ ] S1: <short slice name> — AC: <criterion 1>; <criterion 2>
  - Files touched: <path/to/file.ext> (<new | modified>), ...
- [ ] S2: <short slice name> (depends on S1) — AC: <criterion 1>; <criterion 2>
  - Files touched: <path/to/file.ext> (<new | modified>), ...

## Design Notes
<file-level notes: what's created/changed and why, grounded in recon citations
(cite `file:line`); mark genuinely new ground `<unverified>`>

## Failing Test Stubs
| Slice | AC | Test File | Test Name | Assertion Intent |
|---|---|---|---|---|
| S1 | <criterion 1> | <path/to/test_file> | <test name> | <one-sentence assertion intent> |

## Test Strategy
<per-slice unit / integration / E2E split — this is what qa-engineer uses
to decide which layer a coverage gap belongs in>

## Risks
- S1: <what could go wrong> — noticed by <signal>
